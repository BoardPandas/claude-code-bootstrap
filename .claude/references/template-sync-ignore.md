# Template Sync Ignore List

Files listed here were deliberately removed from this project and must NOT be re-created by the `update-practices` template sync, even though they still exist in the claude-code-bootstrap template.

This file is project-specific. The sync never overwrites it, and local entries are never removed by automation. Add one path per line, relative to the repo root, as a bullet. Include a short reason so future maintainers know why.

## Ignored paths

(none yet)

Example format:

```
- .claude/skills/some-skill/SKILL.md (replaced by project-specific workflow, 2026-07-08)
```
